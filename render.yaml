services:
  - type: web
    name: wulfzx-sitepulse
    runtime: docker
    plan: starter
    disk:
      name: sitepulse-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUBLIC_BASE_URL
        sync: false
